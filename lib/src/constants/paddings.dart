import 'package:flutter/material.dart';

const paddingH4 = EdgeInsets.symmetric(horizontal: 4.0);
const paddingH8 = EdgeInsets.symmetric(horizontal: 8.0);
const paddingH16 = EdgeInsets.symmetric(horizontal: 16.0);
const paddingH32 = EdgeInsets.symmetric(horizontal: 32.0);
const paddingH64 = EdgeInsets.symmetric(horizontal: 64.0);

const paddingV4 = EdgeInsets.symmetric(vertical: 4.0);
const paddingV8 = EdgeInsets.symmetric(vertical: 8.0);
const paddingV16 = EdgeInsets.symmetric(vertical: 16.0);
const paddingV32 = EdgeInsets.symmetric(vertical: 32.0);
const paddingV64 = EdgeInsets.symmetric(vertical: 64.0);

const padding4 = EdgeInsets.all(4.0);
const padding8 = EdgeInsets.all(8.0);
const padding16 = EdgeInsets.all(16.0);
const padding32 = EdgeInsets.all(32.0);
const padding64 = EdgeInsets.all(64.0);

const paddingH4V8 = EdgeInsets.symmetric(horizontal: 4.0, vertical: 8.0);
const paddingH8V16 = EdgeInsets.symmetric(horizontal: 8.0, vertical: 16.0);
const paddingH16V32 = EdgeInsets.symmetric(horizontal: 16.0, vertical: 32.0);
const paddingH32V64 = EdgeInsets.symmetric(horizontal: 32.0, vertical: 64.0);

const paddingH8V4 = EdgeInsets.symmetric(horizontal: 8.0, vertical: 4.0);
const paddingH16V8 = EdgeInsets.symmetric(horizontal: 16.0, vertical: 8.0);
const paddingH32V16 = EdgeInsets.symmetric(horizontal: 32.0, vertical: 16.0);
const paddingH64V32 = EdgeInsets.symmetric(horizontal: 64.0, vertical: 32.0);

const paddingH4V4 = EdgeInsets.symmetric(horizontal: 4.0, vertical: 4.0);
const paddingH8V8 = EdgeInsets.symmetric(horizontal: 8.0, vertical: 8.0);
const paddingH16V16 = EdgeInsets.symmetric(horizontal: 16.0, vertical: 16.0);
const paddingH32V32 = EdgeInsets.symmetric(horizontal: 32.0, vertical: 32.0);
const paddingH64V64 = EdgeInsets.symmetric(horizontal: 64.0, vertical: 64.0);


